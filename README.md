# AI-News

Daily brief on AI governance, built each morning from the AI Narrative Observatory and independently re-sourced.

The live page is the most recent brief. Past briefs are on the archive page.

To publish a new brief: drop the HTML into `_briefs/` as `YYYY-MM-DD.html` and run `python3 build.py`.
